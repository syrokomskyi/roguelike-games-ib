# Extract Tests

Tests for extractor SDK — EXT-001 through EXT-015.

Covers: source safety, deterministic replay, population denominator, staging-only output, identity retention, record loss diagnostics, image evidence, sprite extraction (SDK + BrogueCE sprite pipeline).
