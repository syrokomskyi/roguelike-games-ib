# Extract Tests

Tests for extractor SDK — EXT-001 through EXT-010.

Covers: source safety, deterministic replay, population denominator, staging-only output, identity retention, record loss diagnostics.
