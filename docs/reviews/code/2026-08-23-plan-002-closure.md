# Code Review: PLAN-002 Closure — Fingerprint, Deriver, Coverage

- **Date**: 2026-08-23
- **Base**: `4c78667` (HEAD)
- **Reviewer**: Cascade (fo-review)
- **Files**: 4 changed (3 tracked + 1 new), +26 / -10 code; ~260 generated knowledge-base files (not reviewed)

## Diff scope

| File | Change |
|------|--------|
| `knowledge/sources/bindings.yaml` | Crawl fingerprint + binding_digest updated |
| `scripts/run-stage-coverage.ts` | +16 new coverage dimensions across 3 games; corrected NetHack binding_digest and expected counts; updated Crawl binding_digest |
| `scripts/compute-crawl-fingerprint.ts` | New utility script (untracked) |
| `docs/plans/plan-002-remaining-extraction-work.md` | `closedAt: 2026-08-23`, acceptance criteria checked off |

Generated knowledge-base files (claims, relations, semantic records, coverage JSONL) are deriver output — not code, excluded from review.

## Mechanical Floor

| Check | Result |
|-------|--------|
| Fingerprint verification (`computeSourceFingerprint` vs `bindings.yaml`) | PASS — computed fingerprint `c67eae32...` matches `bindings.yaml` value |
| Binding digest verification (`computeBindingDigest` vs `bindings.yaml`) | PASS — computed digest `ed51ed56...` matches `bindings.yaml` value |
| Coverage binding_digest consistency (all 4 games vs `bindings.yaml`) | PASS — all 4 coverage JSONL files reference the same binding_digest as `bindings.yaml` |
| `tsc --noEmit` | N/A — no package code changed; only scripts and YAML |
| `vitest run tests/cover/` | PASS (pre-existing, no changes to cover tests) |

## Axis A — Structural Correctness

| Item | Status | Notes |
|------|--------|-------|
| Strict typing | PASS | No new `any` or implicit casts. Coverage dimensions use typed `CoverageDimension` via `makeDimension()`. |
| Magic numbers | PASS | Expected/extracted/validated counts are population denominators — same pattern as existing code. Values match extractor manifests exactly. |
| Minimalism | PASS | `compute-crawl-fingerprint.ts` is a 9-line utility script, mirrors existing `compute-catbn-fingerprint.ts` pattern. No unnecessary abstraction. |
| Dead code | PASS | No dead code introduced. |
| Unjustified removals | **FAIL** | `run-stage-coverage.ts` silently changed NetHack `creatures` expected from 379→376 and `items` from 430→454. The old values represented "source tree count" vs "extractor manifest count". No investigation documented for why the previous values were wrong. The change is likely correct (aligning with manifest `expected` values), but the removal of the old mismatch documentation ("376 of 379 extracted (3 missing)", "458 of 430 expected extracted") drops information about source-tree vs extractor discrepancies. |
| Error handling | N/A | Script has no error paths changed. |
| Fowler code smells | PASS | No new smells. Hardcoded paths in scripts are an established codebase pattern (12+ scripts use the same `WORKSPACE` constant). |

## Axis B — DNA Alignment

`forge.yaml` `bindings.paths.invariantsFile` is `null` — **no invariants file**. Invariant alignment skipped.

## Axis C — Ecosystem Fit

| Item | Status | Notes |
|------|--------|-------|
| Package boundaries | PASS | No package imports changed. Scripts import from `@roguelike-games-ib/knowledge-core` — same as existing scripts. |
| Export surface | PASS | No package exports changed. |
| Workspace deps | PASS | No dependency changes. |
| AGENTS.md rules | PASS | No new extractor packages. Coverage dimensions match extractor manifest `exhaustivePopulations` exactly. |

## Axis D — Forward-Only Discipline

| Item | Status | Notes |
|------|--------|-------|
| No silent breaking changes | **FAIL** | `scripts/run-stage13-crawl.ts:25-26` still hardcodes the OLD Crawl fingerprint (`c3f1ce60...`) and binding_digest (`de8b21b4...`). This diff updated `bindings.yaml` and `run-stage-coverage.ts` but did not update `run-stage13-crawl.ts`. If someone runs the crawl extraction staging script, it will produce evidence with a stale binding digest that doesn't match `bindings.yaml`. |
| Migration path | PASS | Coverage file format unchanged — only dimensions added. Old consumers reading 4-dimension crawl coverage will see 8 dimensions (additive). |
| Version bumps | N/A | Scripts are not versioned packages. |

## Axis E — RFC/Plan Compliance

| Item | Status | Notes |
|------|--------|-------|
| PLAN-002 closure | PASS | `closedAt: 2026-08-23` set. All 8 acceptance criteria checked. |
| PLAN-002 status field | **FAIL** | Frontmatter `status` remains `accepted` — should be `closed` or `implemented` to match the `closedAt` date. The plan system expects status to reflect lifecycle state. |
| Coverage completeness | PASS | All new extractor dimensions (Crawl spells/branches/forms/abilities, NetHack artifacts/traps/roles/races/branches/skills, Cataclysm-BN bionics/cb_traps/recipes/cb_skills/effects/factions) are present in coverage files with matching expected/extracted/validated counts. |

## Axis F — Agent Clarity

| Item | Status | Notes |
|------|--------|-------|
| Script discoverability | PASS | `compute-crawl-fingerprint.ts` follows the naming convention of `compute-catbn-fingerprint.ts`. |
| Hardcoded paths | PASS (pre-existing) | All scripts use hardcoded absolute paths. Established pattern — not a regression. |
| Coverage dimension naming | PASS | Dimension IDs match `populationDimension` values in extractor adapters exactly (verified against extractor manifests). |

## Axis G — Test Coverage

| Item | Status | Notes |
|------|--------|-------|
| Existing tests | PASS | `c13-crawl.test.ts` fingerprint test should now pass (was the 1 pre-existing failure). No test changes needed. |
| New test coverage | N/A | No new code paths requiring tests — changes are config/data updates. |
| Regression risk | PASS | Coverage unit tests (`cover-001-005`) test the computation functions, not the specific dimension values. No regression risk from adding dimensions. |

## Findings Summary

| # | Severity | Axis | Finding |
|---|----------|------|---------|
| 1 | **High** | D | `run-stage13-crawl.ts` has stale Crawl fingerprint and binding_digest — will produce evidence with wrong digest if run |
| 2 | **Medium** | E | PLAN-002 `status` field remains `accepted` instead of `closed`/`implemented` despite `closedAt` being set |
| 3 | **Low** | A | NetHack coverage expected counts changed without documenting why old values (379/430) were replaced with manifest values (376/454) |

## Recommendation

Fix findings 1 and 2 before committing. Finding 3 is informational — the new values are correct (match extractor manifests), but the old values represented a different measurement (source-tree count vs extractor-expected count) and the discrepancy information is now lost.
